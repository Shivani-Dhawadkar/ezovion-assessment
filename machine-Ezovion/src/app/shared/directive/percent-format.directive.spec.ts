import { PercentFormatDirective } from './percent-format.directive';

describe('PercentFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PercentFormatDirective();
    expect(directive).toBeTruthy();
  });
});
